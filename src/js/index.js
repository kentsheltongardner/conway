import Manager from './manager.js';
window.addEventListener('load', () => { new Manager(); });
// Left click on, right click off
// Each time we render we can record the size, neighbor count, on rules, off rules, 
// Fully featured editor (rectangle, copy, select, etc)
// BW file import
// Grid position
// Fast render mode (second canvas of exact size, fill rect)
